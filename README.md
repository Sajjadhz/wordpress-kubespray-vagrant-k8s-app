# wordpress-kubespray-vagrant-k8s-app
wordpress phpmyadmin app using vagrant, kubespray and helm

## How to run
cd wordpress-kubespray-vagrant-k8s-app
chmod +x run.sh
sudo ./run.sh

choose first option to create vagrant VMs ang enter number of nodes, or choose second option fi you already have VMs and provide it's IP addresses, username and password.